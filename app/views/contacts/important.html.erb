<h1>All Important Contacts for <%= link_to @user.name, current_user %></h1>
<%= link_to "All Contacts", contacts_path %> | <%= link_to "All Contact Emails", contact_emails_path %>
<table>
<% unless @contacts.empty? %>
	<tr>
		<td colspan="5">
			<%= page_entries_info @contacts %>
			<%= will_paginate @contacts %>
		</td>
	</tr>
<% end %>
	<tr>
		<td>ID</td>
		<td>Name</td>
		<td>Email Addresses</td>
		<td>Unread</td>
		<td></td>
	</tr>
<% unless @contacts.empty? %>
	<%= render @contacts %>
	<tr>
		<td colspan="5">
			<%= will_paginate @contacts %>
		</td>
	</tr>
<% end %>

</table>
